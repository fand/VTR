# TODO

- TD tox rework — Rec page を `/vtr/*`+listen port に、Play page を vtr-player の同期クエリクライアントに(docs/tasks/resolver-server/plan.md Follow-ups)
- editor preview の vtr-player 委譲(resolver を一本化)

Done: resolver-server (vtr-player) 実装 — protocol v2 (`/vtr` namespace、ctrl port 廃止、echo port)、同期クエリ API、パンチイン。docs/tasks/resolver-server/spec.md
