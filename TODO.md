# TODO

- Curve panelのProperty list, address順番でsortするようにして
- Property listでpropertyを選択してるとき、選択されていないcurveは薄く表示するようにして (alpha=0.1くらい)
  - pointの丸も描画しないようにする
- Curveのpointを複数選択したとき、pointを囲むtransform boxを描画する
  - transform boxをドラッグすることでpointを丸ごとmoveできる
  - boxの端をドラッグすると、pointを伸縮できる
