[project]
name = "vtr-td"
version = "0.1.0"
description = "Pure-Python core for the VTR TouchDesigner component (vtr.tox)"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
