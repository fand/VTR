# osc-redit: OSC 記録/再生システム 仕様

ライブVJパフォーマンスのアーカイブ動画を作成するために、リアルタイムで入力されるOSCイベントを記録して連結/編集し、一本のOSCログを生成するシステムを開発する。
OSCイベントの記録は一発通しで行われることも、複数回にわけて行われることもあり、本システムではDAWのようにOSCイベントの記録を編集できるものとする。
出力されたOSCログはTouchDesignerで再生し、高品質でのオフラインレンダリングに利用される。

## 成果物

本システムは以下の部品で構成される:

- osc-tap
  - OSC入力を記録しつつ、VJアプリに転送する
  - 単一障害点なので、将来的にはOSの監視システムを使って復旧できるようにしたい
- osc-editor
  - osc clipを連結/編集して書き出すためのGUIアプリ

## トポロジ

```
TouchOSC (:10010) ──► osc-tap ──生UDP──► TD (:10011)
                        │
                        ▼
                  clip.jsonl ──► osc-editor ──► session.jsonl
                                                      │
                                        TD OSC_REPLAYER(フレームロック)
                                                      │
                                        non-realtime レンダー + ffmpeg mux
```

- osc-tap は **直列インターセプトプロキシ**: 受けたデータグラムをまず無改変で転送し、その後パース済みコピーをログする。転送で再エンコードしてはならない。
- 最終レンダーの再生は**TouchDesigner内のファイル駆動**(フレームロック)。アプリがレンダーと同期してOSCを流すことはない。
- TouchDesignerからosc-tapに時刻同期用の信号が送られることもある

## osc-tap要件

- Rustで実装
- ソケットごとに専用スレッド。転送より先に**単調増加クロック**で到着時刻を打刻する。記録タイミングは TD の cook レートから独立していること
- 実測の受入基準(Python試作、120Hz、TD高負荷時): ギャップ中央値 8.3ms、p99 10.4ms、団子ペア 0。書き直し実装はこれを下回ってはならない
- 1メッセージ=1 JSON行を追記し、毎行 flush(クラッシュしても何も失わない)

## osc-editor要件

- Electron or Rust+eguiで実装
- osc-tapの起動、終了も制御する
- UIはheaderとtracksで構成
- headerにはtimecode, レコード開始ボタン、及び書き出しボタンを持つ
  - レコード開始ボタンを押すと新規トラックにclipを保存
- tracksにはtrackが縦に複数並ぶ
- trackには複数のclipを並べられる
  - clip = 1 record (jsonlファイル)
  - clipの時刻は、osc recordに含まれる、TouchDesignerから送られる時刻シグナル情報を元に配置される
  - clipは移動/trim/削除が可能


## ログ形式(JSONL)— アプリ↔TD の契約

```
{"type":"session_start","t":0.0,"host":"127.0.0.1","routes":["10010->10011"]}
{"t":12.345,"port":10010,"a":"/fader_foam_speed","args":[0.42]}
{"t":12.361,"port":10001,"a":"/kick-on","args":[]}
{"type":"session_end","t":123.4}
```

- `t`: セッション開始からの秒数(単調増加)。`port`: 受信ポート。`a`: OSC
  アドレス。`args`: パース済み引数(float/int/str/bool。blobは `"<blob:N>"`)。
- 1ファイル 1セッション
- 将来のフィールド `tl`(ビーコン由来の、到着時点のTDタイムライン秒): 全
  イベントに付いている場合、再生とマージは `t` ではなく `tl` をキーにする。
- OSC バンドルはパース時に個別メッセージへ展開する。

## TD→アプリ タイムラインビーコン

- TD が `/project1` のタイムライン位置を約10Hzで osc-tap に送り、レコーダーが各イベントに最新値を `tl` として刻む
  - 目的はテイクと音声/タイムラインのドリフトなし整列
- 案: 専用ポートで `/tap/timeline <float seconds>`

## エディタ / マージ

- レーン = OSCアドレス。連続値 → オートメーション曲線(ブレークポイント)、
  引数なし/カウンタ系イベント(`/kick-on`)→ マーカー。
  編集はオフラインのファイル→ファイル処理。
- マージ方針: 各テイクがアドレスの許可リスト(レーン所有権)を宣言する。
  同一アドレスへの重複書き込みは時刻順の後勝ちで解決。
- 出力 = 上記ログ形式の単一 `session.jsonl`。
- エディタはタイミングクリティカルな経路に一切置かない。
